{
  "name": "createflow-dongle",
  "version": "0.1.2",
  "private": true,
  "description": "Open firmware and flasher that turn an nRF52840 USB stick into the wireless dongle for the Naya Create keyboard",
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -b packages/dfu packages/serial && npm run build -w @createflow-dongle/flasher",
    "test": "vitest run",
    "typecheck": "tsc -b packages/dfu packages/serial",
    "flash": "node tools/flash.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vite": "^7.3.6",
    "vitest": "^5.0.0"
  }
}
